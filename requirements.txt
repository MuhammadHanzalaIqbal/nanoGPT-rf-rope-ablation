# Runtime dependencies for the notebook.
# On Kaggle / Colab GPU images, torch + tokenizers + datasets + huggingface_hub
# + kagglehub are already preinstalled and CUDA-matched -- only wandb and
# einops actually need pip-installing in those environments.
# This file lists everything the notebook imports, for a clean local setup.

torch>=2.0
numpy
tokenizers>=0.22
datasets>=3.0
huggingface_hub>=0.30
kagglehub
einops>=0.8
wandb>=0.18
